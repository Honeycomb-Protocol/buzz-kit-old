/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beetSolana from '@metaplex-foundation/beet-solana'
import * as beet from '@metaplex-foundation/beet'
import {
  IndexedReference,
  indexedReferenceBeet,
} from '../types/IndexedReference'

/**
 * Arguments used to create {@link Invitation}
 * @category Accounts
 * @category generated
 */
export type InvitationArgs = {
  invitationId: web3.PublicKey
  bump: number
  guild: web3.PublicKey
  invitedBy: web3.PublicKey
  invited: IndexedReference
}

export const invitationDiscriminator = [33, 100, 20, 1, 94, 190, 111, 106]
/**
 * Holds the data for the {@link Invitation} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class Invitation implements InvitationArgs {
  private constructor(
    readonly invitationId: web3.PublicKey,
    readonly bump: number,
    readonly guild: web3.PublicKey,
    readonly invitedBy: web3.PublicKey,
    readonly invited: IndexedReference
  ) {}

  /**
   * Creates a {@link Invitation} instance from the provided args.
   */
  static fromArgs(args: InvitationArgs) {
    return new Invitation(
      args.invitationId,
      args.bump,
      args.guild,
      args.invitedBy,
      args.invited
    )
  }

  /**
   * Deserializes the {@link Invitation} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [Invitation, number] {
    return Invitation.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Invitation} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<Invitation> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(`Unable to find Invitation account at ${address}`)
    }
    return Invitation.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      '38foo9CSfPiPZTBvNhouNaYpvkzKEzWW396PUW2GKPVA'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, invitationBeet)
  }

  /**
   * Deserializes the {@link Invitation} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [Invitation, number] {
    return invitationBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link Invitation} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return invitationBeet.serialize({
      accountDiscriminator: invitationDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Invitation}
   */
  static get byteSize() {
    return invitationBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Invitation} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      Invitation.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link Invitation} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === Invitation.byteSize
  }

  /**
   * Returns a readable version of {@link Invitation} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      invitationId: this.invitationId.toBase58(),
      bump: this.bump,
      guild: this.guild.toBase58(),
      invitedBy: this.invitedBy.toBase58(),
      invited: this.invited,
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const invitationBeet = new beet.BeetStruct<
  Invitation,
  InvitationArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['invitationId', beetSolana.publicKey],
    ['bump', beet.u8],
    ['guild', beetSolana.publicKey],
    ['invitedBy', beetSolana.publicKey],
    ['invited', indexedReferenceBeet],
  ],
  Invitation.fromArgs,
  'Invitation'
)
