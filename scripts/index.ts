import { Honeycomb } from '@honeycomb-protocol/hive-control';
import { getDependencies } from './utils';

const buzzguild = () => {
    const { connection, mx } = getDependencies('devnet', "guildKit");
}